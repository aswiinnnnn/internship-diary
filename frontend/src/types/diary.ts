export interface DiaryEntry {
  id: string;
  date: string; // ISO date string YYYY-MM-DD
  workSummary: string;
  hoursWorked: string;
  showYourWork: string;
  learnings: string;
  blockers: string;
}
